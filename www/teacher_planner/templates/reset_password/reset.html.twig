{% extends 'base.html.twig' %}

{% block title %}Recupera el teu password{% endblock %}

{% block body %}
    <h1>Recupera el teu password</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Recupera</button>
    {{ form_end(resetForm) }}
{% endblock %}
